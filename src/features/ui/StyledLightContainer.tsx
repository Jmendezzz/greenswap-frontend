import styled from "styled-components";

const StyledLightContainer = styled.div`
    background-color: var(--primary-color-light);
    padding: 1rem;
    border-radius: 1.4rem;

`

export default StyledLightContainer;